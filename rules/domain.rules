# This is an example file for how rules are to be defined
# This `domain.rules` file is a key, valuse pair file, and keys are resolved as:
#   key = value
# with leading and trailing spaces ignored.
# Commented out lines are start with hash, empty lines are ignored.
# If, key need to reject value then it needs to be specified as:
#   key != value


# www_required = strict|optional|deny
# match_querystring = yes|no
# protocol = <protocol1>,<protocol2>...
# ssl_required = yes|no
# ports = <port1>,<port2>...
# port_required = yes|no
# username = <username>
# password = <password>
# username_required = yes|no
# password_required = yes|no
# default_path = <path>
# default_path != <path>
# querystring_param_to_ignore = <param1>,<param2>...
# fragment_ignore = yes|no
# allow_subdomains = yes|no
# allowed_subdomains = <sdomain1>,<sdomain2>...
# allowed_subdomains != <sdomain1>,<sdomain2>...
# can_subdomains_override_this_file = yes|no
# allowed_subdomains_to_override = <sdomain1>,<sdomain2>...
# restrict_subdomains_to_override = <sdomain1>,<sdomain2>...
# ssl_verify = yes|no
# weak_cipher_check = yes|no
